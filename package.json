{
  "name": "forexify",
  "version": "1.0.0",
  "description": "Shopify app displaying ECB exchange rates (DKK/EUR, SEK/EUR) in storefront",
  "scripts": {
    "shopify": "shopify",
    "build": "shopify app build",
    "dev": "shopify app dev",
    "push": "shopify app push",
    "generate": "shopify app generate",
    "deploy": "shopify app deploy"
  },
  "dependencies": {
    "@shopify/app": "^3.50.0",
    "@shopify/cli": "^3.50.0"
  },
  "devDependencies": {},
  "private": true,
  "workspaces": [
    "extensions/*"
  ]
}
